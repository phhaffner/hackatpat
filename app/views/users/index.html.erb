
<h1> liste des soirées à venir <h1>


<table>
  <thead>
    <tr>
      <th>Nom</th>
      <th>Date</th>
      <th>Heure</th>
      <th>Description</th>
      <th>liens</th>
      <th colspan="3"></th>

    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.nom %></td>
        <td><%= event.date %></td>
        <td><%= event.heure %></td>
        <td><%= event.description %></td>
        <td><%= link_to "Paaarty", event %></td>
      </tr>
    <% end %>
  </tbody>
</table>