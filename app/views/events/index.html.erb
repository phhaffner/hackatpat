<p id="notice"><%= notice %></p>

<h1>Liste des soirées</h1>

<table>
  <thead>
    <tr>
      <th>Nom</th>
      <th>Date</th>
      <th>Heure</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.nom %></td>
        <td><%= event.date %></td>
        <td><%= event.heure %></td>
        <td><%= event.description %></td>
        <td><%= link_to 'Go!', event %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event', new_event_path %>
